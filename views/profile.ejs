<!DOCTYPE HTML>
<html>
<head>
	<title>Book Vault</title>
	<meta name="viewport" content="width=device-width,initial-scale=1">
	<link rel="stylesheet" href="stylesheets/screen.css"/>
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
	<link href="https://fonts.googleapis.com/css?family=Proza+Libre" rel="stylesheet">
	<link rel="stylesheet" href="https://use.fontawesome.com/29e6464377.css">
	<script src="scripts/profile.js"></script>
</head>
<body>
	<!-- Menu bar. -->
	<div class="menu">
		<div class="container">
			<div class="flex-wrapper">
				<h2>Profile</h2>
				<div class="button-wrapper">
					<a href="/"><i class="fa fa-home fa-2x" aria-hidden="true"></i></a>
					<a href="/all"><i class="fa fa-book fa-2x" aria-hidden="true"></i></a>
					<a href="/dashboard"><i class="fa fa-sliders fa-2x" aria-hidden="true"></i></a>
					<a href="/logout"><i class="fa fa-sign-out fa-2x" aria-hidden="true"></i></a>
				</div>
			</div>
		</div>
	</div>
	<!-- Information section. -->
	<div class="start">
		<div class="container">
			<!-- Sometimes, a password changing message is needed to be displayed. It is checked here to see if a message is included. -->
			<% if (passwordUpdateMessage != undefined) { %>
			<p class="success-message"><%= passwordUpdateMessage %></p>
			<% } %>
			<p>Here you can check your information. Click 'Edit' to edit any field (email cannot be changed).</p>
			<p class="info-header"><strong>Name:</strong></p>
			<p class="info"><%= user.name %></p>
			<p class="info-header"><strong>Email:</strong></p>
			<p class="info"><%= user.email %></p>
			<p class="info-header"><strong>Address:</strong></p>
			<p class="info"><%= user.address %></p>
			<p class="info"><%= user.city %>, <%= user.state %> <%= user.zip %></p>
			<p class="info-header"><strong>Books added:</strong> <%= user.booksAdded %></p><br/>
			<!-- Two button to allow the user to update information or password. -->
			<button id="edit" class="generic-button">Edit info</button><br/>
			<button id="change-password" class="generic-button">Change password</button>
		</div>
	</div>
	<!-- Footer section. -->
	<div class="footer">
		<hr/>
		<div class="container">
			<p>Page by Amer Hamdan (email:<a href="mailto:amashamdan@gmail.com"> amashamdan@gmail.com</a>)</p>
			<p>Visit my pages:</p>
			<ul class="pages">
				<li><a href="http://amashamdan.github.io/P1-portfolio/" target="blank">Portfolio</a></li>
				<li><a href="https://www.linkedin.com/in/amerhamdan" target="blank">Linkedin</a></li>
				<li><a href="https://github.com/amashamdan" target="blank">Github</a></li>
				<li><a href="http://codepen.io/amashamdan/" target="blank">CodePen</a></li>
			</ul>
			<span style="font-size:0.9em">Copyright © Book Vault 2016. All Rights Reserved</span>
		</div>
	</div>
	<script>
		/* Saved to be used in scripts/profile.js. */
		var csrfToken =  <%- JSON.stringify(csrfToken) %>
		var name = <%- JSON.stringify(user.name) %>;
		var email = <%- JSON.stringify(user.email) %>;
		var address= <%- JSON.stringify(user.address) %>;
		var city = <%- JSON.stringify(user.city) %>;
		var state = <%- JSON.stringify(user.state) %>;
		var zip = <%- JSON.stringify(user.zip) %>;
	</script>
</body>
</html> 